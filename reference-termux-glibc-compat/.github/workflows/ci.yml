name: ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  probes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and run compatibility probes
        run: make check
